# Deployment

All source files are now in `src/` for Cloudflare Workers compatibility.

- Main entry: `src/app.js`
- Convex logic: `src/convex/`
- API docs: `src/PROTECTED_API.md`
- Wrangler config: `src/wrangler.toml`

Deploy with Cloudflare Wrangler from the project root.
